"""Analysis feature — data-analysis tools, service (workspace + algorithms),
and db (DuckDB mechanics + persistence). Also owns the Session facade and the
session registry the MCP/CLI surfaces build on.
"""
